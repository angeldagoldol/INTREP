# ---------------------------------------------------------------------------
# Copy to .env and fill in. NEVER commit .env — it holds live secrets.
# ---------------------------------------------------------------------------

# --- Your business ----------------------------------------------------------
# Shown in the storefront, order emails and legal pages.
BRAND_NAME=dagoldol
LEGAL_BUSINESS_NAME=
SUPPORT_EMAIL=

# Countries you will actually ship to, comma-separated ISO codes.
# PH first — it is the main market.
SHIP_TO_COUNTRIES=PH,US,GB,JP,SG,MY,TH,ID,VN,AU,CA,AE,DE,FR,NL,ES,IT

# Where the store is reachable (used to build Stripe return URLs)
PUBLIC_BASE_URL=http://localhost:3000
PORT=3000

# --- Stripe -----------------------------------------------------------------
# Only needed for international card sales. Leave blank to run PayMongo alone.
# Dashboard -> Developers -> API keys. Use sk_test_... until you go live.
STRIPE_SECRET_KEY=

# Dashboard -> Developers -> Webhooks -> your endpoint -> Signing secret.
# Locally, `npm run stripe:listen` prints one (whsec_...).
STRIPE_WEBHOOK_SECRET=

# Three-letter ISO currency. Must match your catalog prices.
# NOTE: jpy is zero-decimal — see src/money.js before changing this.
CURRENCY=php

# Origins allowed to call /api/* from a browser, comma-separated.
# "*" is fine locally. In production list your storefront's real origin,
# so only your site can create Checkout Sessions on your Stripe account.
ALLOWED_ORIGINS=*

# --- PayMongo (Philippines: GCash, Maya, cards) ------------------------------
# The reason this exists: Stripe does not reliably support GCash / Maya / QR Ph
# in the Philippines, and that is where most local volume is.
#
# Dashboard -> Developers -> API Keys. Use the SECRET key (sk_test_... first).
PAYMONGO_SECRET_KEY=

# Created when you register the webhook. PayMongo does not show it again, so
# store it when it is first returned:
#   curl https://api.paymongo.com/v1/webhooks \
#     -u "$PAYMONGO_SECRET_KEY:" -H "Content-Type: application/json" \
#     -d '{"data":{"attributes":{"url":"https://yourdomain.com/api/paymongo/webhook","events":["checkout_session.payment.paid"]}}}'
PAYMONGO_WEBHOOK_SECRET=

# Wallets to offer. gcash and paymaya are the important two in PH.
PAYMONGO_METHODS=gcash,paymaya,card

# --- VAT --------------------------------------------------------------------
# Catalog prices are VAT-INCLUSIVE shelf prices, so VAT is extracted from them
# for the order email, never added on top. Set VAT_RATE=0 to switch it off.
VAT_RATE=0.12
PRICES_INCLUDE_VAT=true
VAT_LABEL=VAT 12%

# --- Order notification email ----------------------------------------------
# Every paid order is emailed here.
NOTIFY_EMAIL=angelmclorenzdagoldol@gmail.com

# Pick ONE provider. Resend is checked first, then SMTP.

# Option A — Resend (https://resend.com), simplest.
# Free tier sends to your own address from onboarding@resend.dev with no
# domain setup. To send from your own domain, verify it in Resend first.
RESEND_API_KEY=
EMAIL_FROM=onboarding@resend.dev

# Option B — any SMTP server, including Gmail.
# For Gmail you must use an App Password (Google Account -> Security ->
# 2-Step Verification -> App passwords), NOT your normal password.
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=

# --- Stock / sales / funds dashboard ----------------------------------------
# Guards /api/admin/* and the /admin.html dashboard. Business numbers are not
# public. Generate one with:  openssl rand -hex 24
# Leave blank and the dashboard API returns 503 rather than serving anything.
ADMIN_TOKEN=

# Which day a sale is filed under. Manila by default — the main market is PH,
# and on UTC every sale before 8am local would land on the day before.
REPORT_TIMEZONE=Asia/Manila

# --- Legal pages ------------------------------------------------------------
# Rendered by `npm run legal:build` into public/legal/. NEVER committed: this
# repository is public, and an address or TIN in its history cannot be taken
# back. The build refuses to run while any of these is blank.
#
# See legal/README.md. If you are not registered yet, that is the real blocker.
BUSINESS_REG_TYPE=            # dti (sole proprietorship) or sec (corporation)
BUSINESS_REG_NO=              # DTI Business Name certificate no. / SEC reg no.
BUSINESS_TIN=                 # BIR Taxpayer Identification Number
BUSINESS_ADDRESS=             # Full registered address
RETURN_ADDRESS=               # Defaults to BUSINESS_ADDRESS
SUPPORT_PHONE=                # A mobile number is normal and expected in PH
DPO_NAME=                     # Data Protection Officer: a NAMED person
JURISDICTION_CITY=            # Whose courts hear a dispute
COURIERS=                     # Comma-separated, e.g. "J&T Express, DHL"

# One window for every market. PH law requires none; the EU and UK require 14.
RETURN_WINDOW_DAYS=14
DELIVERY_ESTIMATE_PH=3–7 working days
DELIVERY_ESTIMATE_INTL=7–21 working days
