node_modules/
.env
.env.local
*.log
# Holds cost_php — your wholesale supplier costs. This repository is public.
prices.csv

data/orders.jsonl
data/enquiries.jsonl
data/processed-events.json
# Your stock levels are business data, not source.
data/stock.json
.DS_Store

# Rendered legal pages carry your registered address and TIN. Build them with
# `npm run legal:build`; never commit the output.
public/legal/

# Built storefront for the static host. Generated by `npm run storefront:build`.
dist/
